{
  "dependencies": {
    "newman": "^5.3.2",
    "newman-reporter-htmlextra": "^1.22.11"
  },
  "scripts": {
    "test": "newman run postman_collection.json -e postman_environment.json",
    "report": "newman run postman_collection.json -e postman_environment.json -r htmlextra"
  }
}