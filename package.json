{
  "dependencies": {
    "newman": "^5.3.2",
    "newman-reporter-htmlextra": "^1.22.11"
  },
  "scripts": {
    "test": "newman run collection.json -e env.json",
    "report": "newman run collection.json -e env.json -r htmlextra"
  }
}